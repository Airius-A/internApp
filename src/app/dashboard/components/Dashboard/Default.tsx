import {
    Box,
    Typography,
} from "@mui/material";



export default function Default() {

    return (
        <Box>
            <Typography variant="h5">This is login dashboard default page</Typography>
            {/* 这里是公司信息的具体内容 */}
        </Box>
    );
}